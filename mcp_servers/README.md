# mcp_servers

Conectores MCP propios de IA CENTRAL (capa de ejecución, ver ARQUITECTURA.md §2):

- MCP server del propio proyecto Django (para que la IA pueda leer/modificar su propio código).
- MCP/SSH hacia la estación local de Fernando.
- MCP/SSH de solo lectura por defecto hacia los otros servidores existentes de Fernando.

Se implementan en la Fase 3 de la hoja de ruta (ver ARQUITECTURA.md §5). Carpeta vacía por ahora.
